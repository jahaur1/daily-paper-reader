---
title: Nonlinear Bias-Compensated Adaptive Filter and Its Application for Time-Series Prediction
title_zh: 非线性偏差补偿自适应滤波器及其在时间序列预测中的应用
authors: "Yi Peng, Haiquan Zhao, Jinhui Hu"
date: 2026-07-22
pdf: "https://arxiv.org/pdf/2607.19902v1"
tags: ["query:ts-wq"]
score: 9.0
evidence: 面向时间序列预测的非线性自适应滤波与偏差补偿
tldr: 现有非线性自适应滤波算法忽略输入噪声，BCKLMS算法虽处理EIV模型但存在固定字典限制和输出非高斯噪声鲁棒性差的问题。本文提出RFFBCGA算法，基于随机傅里叶偏置补偿框架，采用一般自适应函数，同时缓解输入噪声并增强不同噪声场景下的鲁棒性。在真实时间序列预测中，该方法优于现有算法。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.19902v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 445, \"height\": 863, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.19902v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 553, \"height\": 1508, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.19902v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 546, \"height\": 1544, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.19902v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 626, \"height\": 995, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.19902v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 654, \"height\": 497, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.19902v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 648, \"height\": 832, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.19902v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 628, \"height\": 997, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.19902v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 423, \"height\": 1625, \"label\": \"Figure\"}]"
motivation: 克服BCKLMS算法固定字典导致输入信号表征不足以及输出非高斯噪声下鲁棒性差的问题。
method: 提出RFFBCGA算法，结合随机傅里叶特征与偏置补偿项，并采用一般自适应函数形式。
result: 仿真和真实时间序列预测表明，所提算法在存在输入噪声和输出非高斯噪声时性能优于现有方法。
conclusion: RFFBCGA算法有效解决了非线性EIV模型中输入噪声干扰和鲁棒性不足的问题。
---

## 摘要
大多数现有的非线性自适应滤波算法仅考虑输出噪声，忽略了实际中同样普遍存在的输入噪声。尽管最近提出的偏差补偿核最小均方（BCKLMS）算法解决了非线性变量含误差（EIV）模型中的输入噪声问题，但它仍然存在两个主要限制。首先，使用固定大小的字典限制了网络增长，但也使其无法完全捕捉输入信号的特征。其次，作为一种基于最小均方（LMS）的算法，在输出信号存在非高斯噪声时鲁棒性较差。为了解决这些问题，本文提出了基于通用自适应函数的随机傅里叶偏差补偿滤波器（RFFBCGA）算法。在基于随机傅里叶特征的偏差补偿（RFFBC）框架内，所提算法不仅保持了固定的网络结构并通过BC项有效减轻了输入噪声干扰，还实现了对输入信号表征的改进。此外，通过利用通用自适应（GA）函数的灵活形式，算法在各种噪声场景下的鲁棒性进一步增强。包括真实世界时间序列预测任务在内的大量仿真实验证明了所提方法的优越性。

## Abstract
Most existing nonlinear adaptive filtering algorithms only account for output noise, neglecting the fact that input noise is also prevalent in practice. Although the recently proposed bias-compensated kernel least mean square (BCKLMS) algorithm addresses input noise in the nonlinear errors-in-variables (EIV) model, it still suffers from two major limitations. First, the use of a fixed-size dictionary restricts network growth but also prevents it from fully capturing the characteristics of the input signal. Second, as an least mean square (LMS) based algorithm, it exhibits poor robustness in the presence of non-Gaussian noise in the output signal. To overcome these issues, this paper proposes the random Fourier bias-compensated filter under general adaptive function (RFFBCGA) algorithm. Within the random Fourier feature based bias-compensated (RFFBC) framework, the proposed algorithm not only maintains a fixed network structure and effectively mitigates input noise interference through the BC term, but also achieves improved characterization of the input signal. Moreover, by leveraging the flexible form of the general adaptive (GA) function, the algorithm's robustness across various noise scenarios is further enhanced. Extensive simulations, including real-world time series prediction tasks, demonstrate the superiority of the proposed method.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：大多数非线性自适应滤波算法（如KLMS、KRLS）仅考虑输出噪声，忽略了实际应用中普遍存在的输入噪声（即errors-in-variables, EIV模型）。最近提出的偏差补偿核最小均方（BCKLMS）算法虽然能处理输入噪声，但存在两个主要局限：①固定大小的字典导致无法充分捕捉输入信号的全部特征；②基于二阶矩的LMS准则使得算法在输出信号存在非高斯噪声（如脉冲噪声）时鲁棒性严重下降。
- **整体含义**：本文旨在解决EIV模型下非线性自适应滤波中输入噪声干扰与输出非高斯噪声鲁棒性不足的双重挑战，提出一种同时具备固定网络结构、更好输入表征能力、且能在多种噪声环境下保持优良性能的算法。

### 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：将随机傅里叶特征（RFF）映射与偏差补偿（BC）项相结合，并采用通用自适应（GA）损失函数，构建随机傅里叶偏差补偿通用自适应滤波器（RFFBCGA）。
- **关键技术细节**：
  - **随机傅里叶特征（RFF）**：通过Bochner定理，用有限维（D维）的余弦映射 \( G(\bar{u}_i)=\sqrt{2/D}[\cos(\mathbf{w}_1^\top \bar{u}_i+\theta_1),\dots,\cos(\mathbf{w}_D^\top \bar{u}_i+\theta_D)]^\top \) 近似高斯核函数，使得网络权重维度固定（\( D \times 1 \)），避免核方法中网络结构线性增长的问题。
  - **偏差补偿（BC）项**：在代价函数中加入 \( -\frac{\gamma\sigma_{in}^4}{4}\left(\sum_{m=1}^M \frac{\partial^2 \hat{\vartheta}(\bar{u}_i)}{\partial u_m^2}\right)^2 \) 的期望项，用于补偿输入噪声引起的偏差。通过梯度推导得到包含BC项的更新公式（公式(18)）。
  - **通用自适应（GA）函数**：形式为 \( J_{\text{GA}}(e_i,l,\delta)=\frac{|\delta-2|}{\delta}\left[\left(\frac{(e_i/l)^2}{|\delta-2|}+1\right)^{\delta/2}-1\right] \)，通过调节形状参数 \( \delta \) 可退化为LMS（\( \delta \to 2 \)）、LMLS（\( \delta \to 0 \)）、MCC（\( \delta \to -\infty \)）等常见损失函数。这使得算法能灵活适应不同噪声分布（高斯、BG脉冲、α稳定）。
- **算法流程**：初始化权重为零，采样随机频率和相位；每步输入噪声污染后的数据，计算输出误差；代入GA函数梯度与BC梯度修正权重向量。具体见Algorithm 1。

### 3. 实验设计

- **数据集/场景**：
  - **合成非线性系统**：维度M=1和M=2的两个非线性系统（参考[20]），输入噪声为AWGN（SNR=10dB），输出噪声分别采用：①纯AWGN（SNR_o=30dB）；②AWGN+伯努利-高斯（BG）噪声（\( p_b=0.01, \beta_i=500 \)）；③α稳定噪声（\( \tau=1.0 \)或1.2等）。
  - **真实时间序列**：
    - **太阳黑子时间序列**（1749–2025，来自SIDC）：训练集前3000个样本被加性高斯噪声污染（5dB或10dB），后100个样本计算测试MSE。
    - **蔡氏电路混沌时间序列**：基于物理电路生成，同样训练集被污染（5dB或10dB），后100样本测试。
- **基准方法**：KLMS、BC-KLMS、KMCC、RFF-MCC等。所有方法均在同一噪声环境下比较测试MSE（dB）。
- **对比方式**：每个实验进行30次独立蒙特卡洛运行，取平均测试MSE曲线。

### 4. 资源与算力

论文未明确提及使用的GPU型号、数量、训练时长等算力信息。所有仿真均在CPU上完成的可能性较大（基于梯度下降的滤波算法通常不需要高端GPU）。

### 5. 实验数量与充分性

- **实验组数**：共设计了四大类实验：
  1. 合成M=1系统下的三种噪声场景。
  2. 合成M=2系统下的三种噪声场景。
  3. 太阳黑子时间序列预测（两种噪声水平）。
  4. 蔡氏电路时间序列预测（两种噪声水平）。
  另外，还有对理论定理（Theorem 1和2）的仿真验证（1组）。
- **充分性评估**：实验覆盖了经典非线性系统、不同输入维度、不同输出噪声类型（高斯、BG脉冲、α稳定）及真实数据，对比了多种基准算法。但未进行消融实验（如去掉BC项或GA函数的单独变体对比），且未与更多近期非线性EIV方法（如基于TLS的非线性扩展）比较。整体上实验较为充分，公平性较好（30次独立重复，不同参数调节适当）。

### 6. 论文的主要结论与发现

- 提出的RFFBCGA算法在存在输入噪声和输出非高斯噪声的EIV模型下，相比KLMS、BCKLMS、KMCC、RFFMCC等算法，实现了更快的收敛速度和更低的稳态测试MSE。
- 在真实时间序列预测任务中，RFFBCGA对字典选择不敏感（不同于BCKLMS的随机字典导致性能波动），且在不同噪声水平下均保持最优。
- 理论推导得出的Theorem 1和2（无噪声与有噪声输入的自相关矩阵均等于 \( \frac{1}{D}I \)）得到仿真验证，简化了算法推导过程。
- 算法保证了均值和均方稳定性，给出了步长上界条件。

### 7. 优点

- **方法创新性**：首次将RFF、BC、GA三者结合，同时解决输入噪声和非高斯输出噪声问题，且保持固定网络维度。
- **理论贡献**：给出了均值和均方稳定性分析，推导出自相关矩阵简化定理，降低后续分析复杂度。
- **灵活性**：GA损失函数可自适应多种噪声场景，只需调节单一形状参数\( \delta \)。
- **实验验证充分**：包含合成系统、真实时间序列、多种噪声类型，且与多个主流基线对比。

### 8. 不足与局限

- **输入噪声方差已知假设**：BC项需要\( \sigma_{in}^2 \)作为先验知识，实际中若估计不准会影响补偿效果。
- **步长上界对参数敏感**：稳定性条件依赖于\( \xi \)、\( l \)、\( M \)、\( \sigma_{in}^2 \)等参数，当输入维度高或输入噪声方差大时可能限制步长范围（见Remark 3）。
- **消融实验缺失**：未单独分析BC项和GA函数各自的贡献，也未与非线性TLS或其他EIV方法（如核TLS）对比。
- **非高斯噪声类型有限**：仅测试了BG和α稳定两种，未涉及混合噪声或重尾分布的其他模型。
- **计算资源未报告**：未提供运行时间或复杂度对比，但RFF方法本身复杂度为\( O(D) \)，低于核方法。

（完）
